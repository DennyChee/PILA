# Usage:       import synthetic.forward_numpy, synthetic.generate_timeseries, ...
# Description: PILA synthetic-test framework. Generates synthetic InSAR LOS
#              time-series with known ground-truth source parameters, runs them
#              through the production loader + PILA training, and quantifies
#              parameter recovery (clean / noisy / moving-source / SNR sweep).
# Date:        2026-06-22
